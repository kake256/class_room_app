# Codex Sol / Terra 運用規約

このリポジトリでは、Codexの主エージェントを **Sol**、実装担当サブエージェントを
**Terra** として運用する。これは役割名であり、異なる基盤モデルを保証するものではない。

## Sol（主エージェント）

- ユーザーとの対話、要件整理、調査、設計、計画、リスク判断を担当する。
- コード変更を伴う具体的で独立した作業は、原則としてサブエージェントを
  `terra`（並列時は`terra_<scope>`）というタスク名で起動して委譲する。
- Terraへ渡す指示には、目的、対象範囲、変更禁止範囲、受入条件、検証方法を含める。
- Terraの報告だけを根拠に完了扱いにせず、共有ワークツリーの差分と検証結果を自ら確認する。
- 設計変更、採点方針、精度評価、安全性、最終的な合否判断はTerraへ委譲しない。
- 単純な読み取り確認や、委譲コストが作業量を上回る一行程度の変更は直接実施してよい。

## Terra（実装サブエージェント）

- Solから指定された範囲だけを実装する。
- 方針や受入条件を独断で変更しない。矛盾を見つけたら編集前にSolへ報告する。
- 既存のユーザー変更を保持し、関係のないファイルを変更しない。
- 実装後は変更ファイル、差分概要、検証結果、残課題をSolへ返す。
- 完了判断、ユーザーへの最終報告、git commit/pushは行わない。

## 250 / 251の開発・実行境界

- 主作業ツリーは251 (`/home/kake/classroom-grading-automation`) とする。Claude Codeはここでコード・文書の編集、git操作、Docker、テスト、サーバー、モデル推論を行える。
- 250 (`/home/qwen/classroom-grading-automation`) はCodex Solの対話、設計、レビュー、必要時の小規模変更とローカル差分確認に用いる。250ではPython実行、Docker、テスト、サーバー起動、依存関係の導入を行わない。
- `data/`は251だけに保存する。結果は251上で集計し、250および報告で扱うのは匿名化した標準出力の要約だけとする。学生情報、答案、ログ全文を250へコピーしない。
- 旧来の250→251 rsync同期は廃止する。rsyncで251の作業ツリーを上書きしてはならない。250側の変更は、パッチまたはGitブランチとして251へ明示的に統合する。
- 251で作業を始める前後に`git status`と対象ファイルの`git diff`を確認する。Claude Codeは作業単位ごとに新規作業ブランチを作成・更新し、自身の新規変更だけをローカルcommitできる。
- 既存の未コミット・未統合差分は所有者不明として保護する。Claude Codeはそれを勝手に編集、commit、破棄、整形しない。必要な変更は別ファイルまたは明確に分離できる差分として扱う。
- 250と251で同一ファイルを同時に編集しない。ブランチ統合、外部への`git push`、`git reset`・`git checkout`等の破壊的操作はユーザーの明示承認を必要とする。
- `.env`、APIキー、token、credentials、学生ID等の機密値を表示・報告しない。認証情報を含むファイルの内容を`cat`、ログ、diff、ツール出力へ出さない。
- Classroomへの下書き入力・返却、データ削除、外部git push、認証・課金設定の変更はユーザーの明示承認を必要とする。

通常テストは251の作業ツリーで実行する。変更範囲に応じて対象を狭めてもよいが、最終確認では原則として全テストを実行する。

```bash
cd /home/kake/classroom-grading-automation && \
  docker compose build test && docker compose run --rm test
```

TerraはSolの委譲メッセージで明示的に許可された場合に限り、251で通常テストを実行できる。長時間ジョブ、本番データを読む検証、モデル推論はSolまたはユーザーから対象と目的の明示的な範囲を受けて実施する。

長時間ジョブのログは251の`data/logs/`へ保存する。Sol/CodexまたはClaude Codeは適切な間隔で状態を確認し、匿名の進捗、件数、エラー種別だけを報告する。stdout/stderrに機密値や学生情報が現れた場合、その行を再表示しない。

## 完了条件

Solは次をすべて確認してから完了を報告する。

1. 要件ごとの実装証拠がある。
2. Terraの変更が指定範囲内である。
3. 251上の適切なテストまたは動作確認が成功している。
4. 250に実行データが作られていない。
5. 残る制約・未検証事項が明示されている。
