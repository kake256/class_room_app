# Tailscale オーバーレイ用。cp .env.example .env して値を入れる(.env は gitignore)。
# 認証キー: Tailscale管理コンソール → Settings → Keys → Generate auth key
#   reusable を推奨(コンテナ再作成時も同じキーで認証可能)
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxx
