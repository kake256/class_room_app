"""採点課題・ルーブリック・プロンプト・出力JSONスキーマ。

課題(courseWorkId)ごとに課題文を ASSIGNMENTS に登録する。
ルーブリック本体(3観点)はパラメータ実験系の課題で共通。"""

ASSIGNMENT = """\
RandomForest を用いて car と human の2クラスを識別する実習。
課題1: 木の数(n_estimators)と深さ(max_depth)を変更すると識別境界が
どう変化するかをグラフで確認せよ。
課題2: 識別率が最も高くなるパラメータを求めよ。
結果と考察をまとめてPDFまたはGoogleドキュメントで提出。3点満点。"""

ASSIGNMENT_SVM = """\
線形SVM を用いて car と human の2クラスを識別する実習。
課題1: パラメータ(コストC等)を変更すると識別境界(マージン)が
どう変化するかをグラフで確認せよ。
課題2: 識別率が最も高くなるパラメータを求めよ。
結果と考察をまとめてPDFまたはGoogleドキュメントで提出。3点満点。"""

ASSIGNMENT_ADABOOST = """\
AdaBoost を用いて car と human の2クラスを識別する実習。
課題1: パラメータ(弱識別器の数 n_estimators 等)を変更すると識別境界が
どう変化するかをグラフで確認せよ。
課題2: 識別率が最も高くなるパラメータを求めよ。
結果と考察をまとめてPDFまたはGoogleドキュメントで提出。3点満点。"""

ASSIGNMENT_KANSOU_1 = """\
第1回講義の内容をまとめた上で、感想を提出する課題。
今日の講義内容(機械学習の導入)をまとめ、感想を書く。
Googleドキュメントで提出。3点満点。"""

ASSIGNMENT_TOKUBETSU_0511 = """\
特別講義「AIエージェント: その概念と実装のフロンティアを探る」(5月11日)の
内容のまとめと感想を提出する課題。"""

ASSIGNMENT_SUKINA_SHUHO = """\
Regression Forest の講義動画を視聴した上で、
「ここまで学んだ機械学習の中で好きな手法とその理由」をまとめる課題。
どの手法を選んだか、その手法の内容の説明、選んだ理由(自分の考え)を
含むことが期待される。3点満点。

注意(採点上の解釈):
- Regression Forest の動画視聴は課題の前提にすぎず、答案が
  Regression Forest に言及する必要はない。講義で学んだ任意の手法でよい
- 「好きな手法」は1つに限らない。複数の手法を挙げていても、それぞれを
  好きな理由が論理的に説明されていれば減点しない"""

ASSIGNMENT_AI_KANSOU = """\
映像「人工知能 天使か悪魔か？」と関連講義を踏まえ、ディスカッションした内容と
自分の感想・考えをGoogleドキュメントにまとめる課題。3点満点。"""

ASSIGNMENT_APPLICATION_RESEARCH = """\
機械学習を用いた画像認識・画像生成技術が社会実装されている事例を調査し、
どのように機械学習技術が使われているかをまとめ、情報ソースを明記する課題。"""

ASSIGNMENT_DISTANCE = """\
実習サイトの課題02(ユークリッド距離)と03(マハラノビス距離)によるクラス識別に
取り組み、変更したプログラム箇所の説明、結果、考察をまとめる実習課題。3点満点。
設問は以下のとおり:
- 02課題1: 値[2000.0, 40.0]はcar/humanどちらに属するか。ユークリッド距離を
  表示してどちらのクラスに識別されるか説明せよ
- 03課題1: ユークリッド距離とマハラノビス距離の結果を比較し、結果が異なる
  理由をまとめよ
- 03課題2: 値[2000.0, 40.0]はcar/humanどちらに属するか。マハラノビス距離を
  用いて識別せよ
注意: 識別率の一覧比較や「最良パラメータの特定」はこの課題では要求されていない。"""

ASSIGNMENT_KNN = """\
実習サイトの課題04(k最近傍法による教師あり学習)に取り組み、結果と考察を
まとめる実習課題。3点満点。設問は以下のとおり:
- 課題1: kの値を変化させて、クラスの境界線の変わり方を確認せよ
- 課題2: kNNは全プロトタイプとの距離計算で時間がかかる。対策のkd-tree法を調査せよ
注意: 境界線の変化は定性的な観察でよく、識別率の数値一覧や「最良のkの特定」は
この課題では要求されていない。"""

ASSIGNMENT_FACE_DETECTION = """\
顔検出器が誤検出する画像、または顔として検出される似顔絵を作成・確認し、
その結果から顔検出器について分かることを文書にまとめる観察課題。3点満点。"""

ASSIGNMENT_MLP = """\
Neural Network(多層パーセプトロン)の講義動画を視聴し、講義内容について
質問できるAIチューター「藤吉AI先生」への質問とその回答をまとめる課題。
どのような質問をしたか、AIの回答の整理、それに対する自分の理解・考えを
含むことが期待される。3点満点。"""

# 記号キー → (課題文, ルーブリック種別)。汎用定義のみ(courseWorkId等の
# インスタンス固有IDはここに書かない)。実際のcourseWorkId→キーの対応は
# config.yaml の assignments: に置く(gitignore対象、個人情報を公開しないため)。
ASSIGNMENT_SPECS = {
    "rf": (ASSIGNMENT, "EXPERIMENT"),
    "svm": (ASSIGNMENT_SVM, "EXPERIMENT"),
    "adaboost": (ASSIGNMENT_ADABOOST, "EXPERIMENT"),
    "kansou1": (ASSIGNMENT_KANSOU_1, "KANSOU"),
    "tokubetsu0511": (ASSIGNMENT_TOKUBETSU_0511, "KANSOU"),
    "sukina": (ASSIGNMENT_SUKINA_SHUHO, "EFFORT"),
    "sukina_kansou": (ASSIGNMENT_SUKINA_SHUHO, "KANSOU"),  # 好きな手法を感想文想定で採点
    "ai_kansou": (ASSIGNMENT_AI_KANSOU, "KANSOU"),
    "mlp": (ASSIGNMENT_MLP, "EXPERIMENT"),          # 比較用(実験レポート想定)
    "mlp_kansou": (ASSIGNMENT_MLP, "KANSOU"),       # 感想文・まとめ想定(本命)
    "application_research": (ASSIGNMENT_APPLICATION_RESEARCH, "RESEARCH"),
    "distance": (ASSIGNMENT_DISTANCE, "DISTANCE"),  # 専用基準(汎用EXPERIMENTは要求不一致)
    "knn": (ASSIGNMENT_KNN, "KNN"),                 # 専用基準(同上)
    "face_detection": (ASSIGNMENT_FACE_DETECTION, "OBSERVATION"),
}

RUBRIC = """\
## ゲート条件(満たさなければ合計0点)
- 実験結果の提示(識別境界のグラフ、または識別率の数値)が存在すること

## 加点観点(各1点、合計0〜3点)

1. 定量的評価と課題2への回答(1点)
   - 1点の条件(すべて必須): (a)複数条件の識別率が数値で提示・比較されている、
     (b)最良のパラメータの値と識別率の組が答案から特定できること。
     「最良は n_estimators=X で識別率 Z%」のような本文での宣言のほか、
     表・グラフ中で最良の条件と識別率が一意に読み取れる場合
     (識別率の最大値が明確に特定できる表など)も(b)を満たすとみなし、
     本文での再宣言は必須としない
   - (a)はあるが、どの条件が最良か答案から一意に特定できない → 0.5点
   - 数値が1条件のみ、または数値なし → 0点
   - 図・表・キャプションでの提示も本文と同等に認める

2. 実験方法と定性的評価(1点)
   - 何を変更したか(パラメータやコード箇所)の説明があり、
     識別境界の変化を自分の言葉で記述している → 1点
   - 条件を系統的に変えた複数のグラフが整理されて提示されていれば、
     本文の説明が短くても達成とみなす(図リッチな答案を不利にしない)
   - グラフの羅列のみで、何を変えたかの説明が全くない → 0.5点以下

3. 考察・自分の意見(1点)
   - 1点の条件: 観察結果に対する「なぜそうなるか」のメカニズムに踏み込んだ
     理由づけ(なぜ境界が滑らかになる/複雑になるか)を、数文以上の自分の言葉で
     述べている。過学習・汎化・アンサンブル等の概念と結びつけていればなお良い
   - 観察事実の言い換え(「木を増やすと境界が複雑になった」だけ)や、
     教科書的な一般論の引き写しは 0.5点以下
   - この観点のみ文章を要求する。図の提示だけでは加点しない
   - 「面白かった」等の感想のみは 0点

{stance}

- total は観点スコアの和を四捨五入した整数(0〜3)
- 遅延(late)の減点は行わない(システム側で処理する)
- 「特に面白い・参考になる記述」は点数に含めず notable に引用として出力する"""

RUBRIC_KANSOU = """\
## ゲート条件(満たさなければ合計0点)
- 講義に関するまとめまたは感想の記述が存在すること(白紙・無関係な内容は0点)

## 加点観点(各1点、合計0〜3点)
※出力するJSONの観点名は固定( quantitative / method / discussion )だが、
  この課題では以下の意味で採点する:

1. quantitative = まとめの具体性(1点)
   - 講義で扱われた具体的なトピック・技術名・事例に言及しながら
     内容をまとめている → 1点
   - まとめはあるが「AIについて学んだ」のような抽象的な言及のみ → 0.5点
   - まとめがない → 0点

2. method = 理解の正確さと構成(1点)
   - 講義内容を自分の言葉で正しく整理・要約している → 1点
   - 簡略だが本質的に正しい仕組みの説明が一言でもある場合は0点にせず
     0.5点を与える(例: Random Forestを「多くの木の多数決で答えを出す」、
     k近傍法を「近いデータを探して分類する」、SVMを「余裕のある境界線を
     引く」と述べる等。詳細さは問わない) → 0.5点
   - 注意: 上の0.5点は「一言しか触れていない答案への最低保証」であり、
     簡潔な答案の上限ではない。複数の文で仕組みを自分の言葉で正しく
     説明していれば、表現が簡潔でも1点を与える
   - 講義資料の言葉の書き写し中心、または誤解がある → 0.5点
   - 手法名やトピック名を挙げているだけで、仕組みに触れる記述が一切ない
     (「好きだから」「理解しやすかったから」等、理由のみで
     中身の説明がない場合を含む) → 0点

3. discussion = 感想・自分の考え(1点)
   - 具体的な感想に加え、自分の意見・疑問・今後の学習や将来との
     結びつけがある → 1点
   - 「面白かった」「勉強になった」等の一般的な感想のみ → 0.5点
   - 感想がない → 0点

## 取り組み姿勢の扱い
- 課題に誠実に取り組んだ形跡(相応の分量、自分の言葉での記述、具体例を
  挙げようとする姿勢)が明らかな答案は、細部の不足を過度に減点せず
  好意的に解釈してよい。やる気が見える答案を、形だけの答案と同じ帯に
  落とさないこと
- ただし分量だけで内容が伴わない答案への加点はしない

{stance}

- total は観点スコアの和を四捨五入した整数(0〜3)
- 遅延(late)の減点は行わない(システム側で処理する)
- 「特に面白い・参考になる記述」は点数に含めず notable に引用として出力する"""

RUBRIC_EFFORT = """\
## 採点方針(取り組み量+概念理解の軽い確認)
この課題は「ある程度しっかり書いていて、講義で扱った概念をある程度理解して
いれば満点(3点)」とする。専門的な正確さや独自性までは求めないが、
選んだ手法の仕組みを取り違えていたり、感覚的な理由(「強そう」「なんとなく」等)
だけで概念への言及がない場合は満点にしない。

## ゲート条件(満たさなければ合計0点)
- 課題テーマに沿った記述が存在すること(白紙・一文のみ・無関係な内容は0点)

## 加点観点(各1点、合計0〜3点)
※出力するJSONの観点名は固定( quantitative / method / discussion )だが、
  この課題では以下の意味で採点する:

1. quantitative = テーマへの言及(1点)
   - 課題が求める対象(選んだ手法、講義トピック等)に具体的に言及している → 1点
   - 対象が曖昧、または触れていない → 0.5点以下

2. method = 概念理解(1点)
   - 選んだ手法・トピックの仕組みや特徴を、講義で扱った概念に沿って
     おおむね正しく説明している(平易でよい) → 1点
   - 説明が感覚的な印象だけ(「強そう」「万能感」等)で概念に触れていない、
     または明らかな誤解がある → 0.5点以下
   - 説明がほぼない(単語の列挙のみ) → 0点

3. discussion = 分量と自分の言葉(1点)
   - 数文以上のまとまった分量で、自分の言葉で理由や考えを述べている → 1点
   - 極端に短い(1〜2文程度)、明らかな手抜き → 0.5点以下

- 概念理解が示されていれば内容が平易でも3点でよい。減点は「概念への言及が
  なく感覚的」「明らかな誤解」「明らかな分量不足」のいずれかがある場合のみ
- total は観点スコアの和を四捨五入した整数(0〜3)
- 遅延(late)の減点は行わない(システム側で処理する)
- 「特に面白い・参考になる記述」は点数に含めず notable に引用として出力する"""

RUBRIC_DISTANCE = """\
## ゲート条件(満たさなければ合計0点)
- 距離計算による識別への取り組み(テスト点の識別結果、距離の計算、
  プログラム変更のいずれか)の記述が存在すること

## 加点観点(各1点、合計0〜3点)
※出力するJSONの観点名は固定( quantitative / method / discussion )だが、
  この課題では以下の意味で採点する:

1. quantitative = テスト点の識別結果(1点)
   - テスト点[2000.0, 40.0]の識別結果を、ユークリッド(02)とマハラノビス(03)の
     両方について、根拠(計算した距離の値、グラフ上の位置等)とともに示している → 1点
   - 片方の距離のみ、または識別結果だけで根拠がない → 0.5点
   - テスト点の識別に触れていない → 0点
   - この課題に識別率の一覧や最良パラメータは存在しないため、その欠如を理由に
     減点しない

2. method = プログラム変更の説明(1点)
   - 変更・追加したプログラム箇所をコードまたは説明で示し、何をしたかが分かる → 1点
   - コードの貼り付けのみで説明がない、または断片的 → 0.5点
   - プログラム変更への言及がない → 0点

3. discussion = 距離尺度の比較考察(1点)
   - ユークリッドとマハラノビスの結果の違い(または一致)の理由を、データの
     分布・ばらつき・共分散の考え方と結びつけて自分の言葉で説明している → 1点
   - 結果の違いへの言及や定義の説明はあるが、理由づけが定義の書き写し中心 → 0.5点
   - 比較考察がない → 0点

{stance}

- total は観点スコアの和を四捨五入した整数(0〜3)
- 遅延(late)の減点は行わない(システム側で処理する)
- 「特に面白い・参考になる記述」は点数に含めず notable に引用として出力する"""

RUBRIC_KNN = """\
## ゲート条件(満たさなければ合計0点)
- k-NN実習への取り組み(kの変更、境界の観察、kd-tree調査のいずれか)の
  記述が存在すること

## 加点観点(各1点、合計0〜3点)
※出力するJSONの観点名は固定( quantitative / method / discussion )だが、
  この課題では以下の意味で採点する:

1. quantitative = 課題1への回答: 境界変化の観察(1点)
   - kを複数(2つ以上)の値に変えた実行結果(境界のグラフ等)を提示し、
     境界がどう変わったかを具体的に記述している → 1点
   - 1条件のみ、または変化の記述が曖昧 → 0.5点
   - 境界変化の観察がない → 0点
   - 識別率の数値一覧や「最良のk」の特定はこの課題では要求されていないため、
     その欠如を理由に減点しない

2. method = 変更箇所とkの解釈(1点)
   - 何を変更したか(n_neighbors等)を示し、kの大小と境界の複雑さ・
     過学習/未学習の関係に触れている → 1点
   - 変更箇所の提示のみ、または解釈のみ → 0.5点
   - どちらもない → 0点

3. discussion = 課題2への回答: kd-tree法の調査(1点)
   - kd-tree法がどのような仕組みで、なぜ距離計算の高速化になるかを
     自分の言葉で説明している → 1点
   - kd-treeの名前や一般論への言及のみ → 0.5点
   - kd-tree調査がない → 0点

{stance}

- total は観点スコアの和を四捨五入した整数(0〜3)
- 遅延(late)の減点は行わない(システム側で処理する)
- 「特に面白い・参考になる記述」は点数に含めず notable に引用として出力する"""

RUBRIC_RESEARCH = """\
## ゲート条件(満たさなければ合計0点)
- 課題に沿った社会実装事例の調査記述が存在すること

## 加点観点(各1点、合計0〜3点)
※JSON観点名 quantitative / method / discussion を以下の意味で使う:
1. quantitative = 事例の具体性
   - 実在する製品・サービス・利用場面を具体的に特定している → 1点
   - 分野の一般論だけ → 0.5点以下
2. method = 技術説明と情報源
   - 機械学習技術がどう使われるかを説明し、確認可能な情報源を明記 → 1点
   - 技術説明または情報源の一方が不足 → 0.5点以下
3. discussion = 社会実装への考察
   - 効果、制約、課題などを自分の言葉で考察 → 1点
   - 調査内容の紹介のみ → 0.5点以下

{stance}
- totalは観点スコアの和を四捨五入した整数(0〜3)
- 遅延減点はシステム側で処理する"""

RUBRIC_OBSERVATION = """\
## ゲート条件(満たさなければ合計0点)
- 課題に沿った実行・観察結果の記述が存在すること

## 加点観点(各1点、合計0〜3点)
※JSON観点名 quantitative / method / discussion を以下の意味で使う:
1. quantitative = 観察対象と結果の具体性
   - 使用した画像・対象と検出／誤検出結果が具体的 → 1点
   - 結果が曖昧 → 0.5点以下
2. method = 取り組み方法
   - 何を試し、どのような結果になったかを再現可能な程度に説明 → 1点
   - 方法の説明が不足 → 0.5点以下
3. discussion = 結果から分かること
   - 検出器の特徴・限界を結果と結びつけて自分の言葉で考察 → 1点
   - 感想または結果の言い換えのみ → 0.5点以下

{stance}
- totalは観点スコアの和を四捨五入した整数(0〜3)
- 遅延減点はシステム側で処理する"""

STANCE_STRICT = """\
## 採点姿勢(重要)
- 各観点は0点から始め、答案中に明確な根拠(引用できる記述・図)が
  ある場合のみ加点する。好意的な推測で加点しない
- 満点(合計3点)は「特に優れた答案」にのみ与える。標準的にこなした答案は2点が目安
- 部分的にしか満たさない観点は 0.5点を積極的に使う
- 判断に迷う場合は低い方のスコアを付け、あわせて flags に理由を記載する"""

STANCE_LENIENT = """\
## 採点姿勢(重要: この課題はやや甘めに採点する)
- 明らかな不足がない限り、観点は満たしているとみなして加点する(好意的に読む)
- 課題の要求に誠実に取り組んだ形跡があれば、表現の拙さでは減点しない
- 判断に迷う場合は高い方のスコアを付ける
- 0.5点は明確な欠落があるときのみ使う"""

SYSTEM_PROMPT = "あなたは大学の機械学習講義のTAで、提出レポートを厳密かつ公平に採点します。"

RUBRIC_GENERIC = """
この課題は教師が別途明示する0〜3点の判断条件で総合評価する。
課題固有の実験、手法、数値、用語を推測で要求しない。

- gate: 提出物が答案として成立し、与えられた課題に関連する場合にpass。
- quantitative / method / discussion: JSON形式を維持するための中立な補助観点。
  各観点を課題固有の必須条件とみなさず、答案の根拠・取り組み・説明を中立に記録する。
- 最終的な0〜3点の判定は、後続の「教師が確認済みの課題別採点条件」を最優先する。
"""

# rubric キー → テンプレート。EFFORT は採点方針を内蔵し {stance} を持たない
RUBRIC_TEMPLATES = {
    "GENERIC": RUBRIC_GENERIC,
    "EXPERIMENT": RUBRIC,
    "DISTANCE": RUBRIC_DISTANCE,
    "KNN": RUBRIC_KNN,
    "KANSOU": RUBRIC_KANSOU,
    "EFFORT": RUBRIC_EFFORT,
    "RESEARCH": RUBRIC_RESEARCH,
    "OBSERVATION": RUBRIC_OBSERVATION,
}


def resolve_assignment(
    coursework_id: str | None, assignments_map: dict[str, str] | None
) -> tuple[str, str]:
    """courseWorkId → (課題文, ルーブリック種別)。

    assignments_map は config.yaml の assignments: (courseWorkId → 記号キー)。
    coursework_id が None(calibrate等)のときは既定(rf)を使う。
    """
    if coursework_id is None:
        return ASSIGNMENT_SPECS["rf"]
    key = (assignments_map or {}).get(coursework_id)
    if key is None:
        raise KeyError(
            f"courseWorkId {coursework_id} が config.yaml の assignments: に未登録です。"
            f"assignments に '{coursework_id}: <キー>' を追加してください"
            f"(利用可能なキー: {', '.join(ASSIGNMENT_SPECS)})。"
        )
    if key not in ASSIGNMENT_SPECS:
        raise KeyError(
            f"assignments の '{coursework_id}: {key}' のキー '{key}' が未定義です"
            f"(利用可能: {', '.join(ASSIGNMENT_SPECS)})。"
        )
    return ASSIGNMENT_SPECS[key]


# 既定スタンスが甘め(lenient)のルーブリック種別。感想文系は教員方針として
# 取り組み重視で甘めに採点する(検証: strict比でbias -0.43→-0.25等、2026-07-20)。
# CLIの --strict / --lenient 指定は常にこの既定を上書きする
DEFAULT_LENIENT_RUBRICS = {"KANSOU"}


def build_user_prompt(
    assignment: str, rubric_key: str = "EXPERIMENT", lenient: bool | None = None
) -> str:
    rubric_template = RUBRIC_TEMPLATES[rubric_key]
    if lenient is None:
        lenient = rubric_key in DEFAULT_LENIENT_RUBRICS
    if "{stance}" in rubric_template:
        rubric = rubric_template.format(stance=STANCE_LENIENT if lenient else STANCE_STRICT)
    else:  # EFFORT等、採点方針内蔵のルーブリックは stance を差し込まない
        rubric = rubric_template
    return f"""以下のレポート課題の提出物(ページ画像)を、ルーブリックに従って採点してください。

# 課題
{assignment}

# ルーブリック
{rubric}

# 出力指示
- 指定のJSONスキーマに従い、JSONのみを出力すること
- criteria は観点1〜3を name: "quantitative" / "method" / "discussion" の順で3件出力すること
- evidence は本文の短い引用、またはページ番号+図の説明(例: "p.2 の3条件の境界比較図")とする。
  実在しない内容を書かないこと。加点する観点には必ず具体的な根拠を書くこと
- 判断に迷った場合の扱いは上記「採点姿勢」に従い、flags に理由を記載すること"""

GRADING_SCHEMA = {
    "type": "object",
    "properties": {
        "gate": {
            "type": "object",
            "properties": {
                "pass": {"type": "boolean"},
                "reason": {"type": "string"},
            },
            "required": ["pass", "reason"],
        },
        "criteria": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "name": {"type": "string"},
                    "score": {"type": "number"},
                    "evidence": {"type": "string"},
                    "comment": {"type": "string"},
                },
                "required": ["name", "score", "evidence", "comment"],
            },
        },
        "total": {"type": "number"},
        "flags": {"type": "array", "items": {"type": "string"}},
        "notable": {"type": ["string", "null"]},
    },
    "required": ["gate", "criteria", "total", "flags", "notable"],
}

CRITERIA_NAMES = ["quantitative", "method", "discussion"]

# 講義で扱う技術用語(感想文系課題の「具体性」を測る補助指標に使用。report.py参照)。
# 新しい課題・手法を追加したらここにも追記すること。
COURSE_KEYWORDS = [
    "RandomForest", "Random Forest", "ランダムフォレスト", "決定木",
    "SVM", "サポートベクター", "マージン", "カーネル",
    "AdaBoost", "アダブースト", "弱識別器", "弱識別機", "強識別器",
    "kNN", "k近傍", "最近傍", "プロトタイプ法",
    "HOG", "顔検出", "特徴量", "局所特徴",
    "Regression Forest", "回帰森",
    "n_estimators", "max_depth", "パラメータ",
    "過学習", "汎化", "アンサンブル", "識別境界", "識別率", "交差検定",
]
